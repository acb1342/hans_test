<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="adminLogin">

    <!-- 패스워드 조회 -->
    <select id="selectPasswd" parameterType="map" resultType="String">
        SELECT /* adminLogin.selectPasswd */
            PASSWD as passwd
        FROM TB_MGMT_ADMIN
        WHERE ADMIN_ID = #{adminId}
    </select>
    
    
    <!-- 패스워드 수정 -->
    <update id="updatePasswd" parameterType="map">
        UPDATE /* adminLogin.updatePasswd */
        TB_MGMT_ADMIN
            SET PASSWD = #{newPasswd}
        WHERE ADMIN_ID = #{adminId}
    </update>
    
    
    
</mapper>